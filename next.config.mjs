/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
    images: {
        domains: ['backend.rplsmknusa.com'],
        unoptimized: true,
      },
      
};

export default nextConfig;
